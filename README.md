# Flutter Builder

Repository untuk build APK Flutter via GitHub Actions.

## Workflow

- `release-build.yml` - Build APK Release
- `debug-build.yml` - Build APK Debug
- `web2apk.yml` - Build APK dari website

## Maintainer

@limitedkyzenchenkiorbot
